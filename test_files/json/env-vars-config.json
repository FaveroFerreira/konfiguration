{
  "server_port": "${SERVER_PORT}",
  "database": {
    "host": "postgres://localhost:5432/db",
    "username": "postgres",
    "password": "${POSTGRES_PASSWORD}"
  },
  "api": {
    "url": "https://api.example.com",
    "token": "${API_TOKEN}"
  }
}